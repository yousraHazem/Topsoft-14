<%= form_for(@registered_user) do |f| %>
  <% if @registered_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registered_user.errors.count, "error") %> prohibited this registered_user from being saved:</h2>

      <ul>
      <% @registered_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
