<%= form_for(@survey) do |f| %>
  <% if @survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @survey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_question %><br>
    <%= f.text_field :survey_question %>
  </div>
  <div class="field">
    <%= f.label :survey_answers %><br>
    <%= f.text_field :survey_answers %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
