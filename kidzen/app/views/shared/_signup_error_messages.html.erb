<% if ! @errors.empty? %>
  <div id="error_explanation">
    <div class="alert">
      The form contains <%= pluralize(@errors.length, "error") %>.
    </div>
    <ul>
      <% @errors.each do |msg| %>
        <li>*<%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

