<%= form_for(@poll) do |f| %>
  <% if @poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poll.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% @poll.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :poll_question %><br>
    <%= f.text_field :poll_question %>
  </div>
  <div class="field">
    <%= f.label :poll_answers %><br>
    <%= f.text_field :poll_answers %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
