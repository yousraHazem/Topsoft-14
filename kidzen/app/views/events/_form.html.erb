<%= form_for(@event) ,  html: { multipart: true } do |f| do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date_time %><br>
    <%= f.datetime_select :date_time %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <p>
   <%= f.label :photoDescription %><br />
   <%= f.text_field :photoDescription %>
  </p>
   <p>
   <%= f.label :image,"Upload your Photo from Here" %> <br />
   <%= f.file_field :image, :id => "loc", :onclick => "doLoc()" %>
  </p>
  <script type="text/javascript">
  function doRem() {
    document.getElementById("loc").value = '';
  }
  </script>

  <div class="actions">
    <%= f.submit %>
  </div>


<% end %>